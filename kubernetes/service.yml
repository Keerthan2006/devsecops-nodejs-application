apiVersion: v1
kind: Service
metadata:
  name: nodeapp-svc
  namespace: application
spec:
  type: NodePort
  selector:
    app: node
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001
